.venv/
node_modules/
__pycache__/
*.py[cod]
.pytest_cache/
.coverage
dist/
*.egg-info/
.env
.env.platform
.env.local

# Local R2 truth store (dev/runtime; INSERT-only spike output)
.ihl-local-r2/
**/.ihl-local-r2/

# knowledge_search.py --semantic のベクトルインデックス(再生成可能な派生物)
**/.vector-index/

# Playwright e2e 実行時の生成物(再生成可能・機材依存)
test-results/
**/test-results/

# Claude Code ローカル権限設定(マシン固有パス。settings.json 本体はコミット対象)
.claude/settings.local.json

# Env legacy archive (must never commit secrets)
# Local walkthrough junction (setup-mockups-link.mjs → ../mockups; do not duplicate PNGs)
02-設計/_ui-global/ux-walkthrough/mockups/

# Scratch/one-off patch scripts and their output (not meant for commit)
docs/planning/w2-checkpoint/_tmp_*
docs/planning/w2-checkpoint/_patch_docs.py

