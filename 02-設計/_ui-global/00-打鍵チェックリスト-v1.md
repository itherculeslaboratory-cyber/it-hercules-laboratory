# 00 打鍵チェックリスト v1（ユーザー手動 · Tier D 前）

> **作成日**: 2026-06-09  
> **対象**: `apps/web`（Next.js 15）+ FastAPI `apps/api`  
> **前提**: `docker compose up api` + `cd apps/web && npm run dev` または `docker compose --profile web up`  
> **記号**: `[ ]` はユーザーがキーボード操作で確認後に `[x]` にする

---

## 0. 起動確認

- [ ] `http://localhost:8000/health` → `{"status":"ok"}`
- [ ] `http://localhost:3000/` → ホーム（司令塔）が表示される
- [ ] Tab キーでヘッダーナビ・主 CTA にフォーカスが届く

---

## 1. オンボーディング（01 / 02 / 03 / 21）

| # | ルート | 操作 | 期待 |
|---|--------|------|------|
| O1 | `/login` | メール入力 → Enter → 送信 | 送信済メッセージ（開発モード） |
| O2 | `/register` | handle・言語・規約チェック → 登録 | ホームへ遷移 or 成功 |
| O3 | `/terms` | 条文スクロール | **草案**バッジ表示 |
| O4 | `/register` 内 | 言語 select を Tab で変更 | フォーカスリング可視 |

---

## 2. ホーム・司令塔（04）

| ルート | 操作 | 期待 |
|--------|------|------|
| `/` | Tab → 主 CTA「観測をはじめる」→ Enter | `/observation` |
| `/` | 要約カード 4 枚のいずれか → Enter | 各 href 先へ |

---

## 3. 観測（05）

| ルート | 操作 | 期待 |
|--------|------|------|
| `/observation` | フィルタ入力 → 絞り込む | 空状態 or グリッド（Parquet 有無） |
| `/observation` | 計測入力リンク | `/observation/input` |
| `/observation/input` | 雄/雌トグル → 行追加 → 保存 | 成功メッセージ |
| `/observation/templates` | テンプレカード → Enter | 詳細 |
| `/observation/templates/tpl_male_std` | このテンプレで記録 | `/observation/input` |

---

## 4. マーケット・GMO（06 / 23）

| ルート | 操作 | 期待 |
|--------|------|------|
| `/market` | タブ（出品/オークション） | 一覧更新 |
| `/market/lst_01` | 申し込むボタン | Stage 1 ボード表示 |
| `/market/lst_01/transfer` | — | **stub tier** バッジ・振込コード |

---

## 5. 掲示板・論文・争い（07 / 09 / 11 / 19）

| ルート | 操作 | 期待 |
|--------|------|------|
| `/board` | 板を選択 | `/board/{category}` |
| `/board/general` | 投稿する | フォーカス可能 |
| `/board/general/dispute?thread=thr_g01` | — | 公開観覧メッセージ |
| `/board/paper` | 参加 / テンプレ | 遷移可 |
| `/board/component` | — | issue 一覧 or 空状態 |

---

## 6. 好み・プロフィール・貢献（10 / 08 / 14）

| ルート | 操作 | 期待 |
|--------|------|------|
| `/match` | 左右いずれか → こちら | 記録メッセージ |
| `/me/profile` | — | カルマ・貢献度・評価 3 カード |
| `/contribution` | — | PT 残高・バッジ |

---

## 7. 血統（03）

| ルート | 操作 | 期待 |
|--------|------|------|
| `/cross/cross_01` | 死亡一覧へ | `/cross/cross_01/mortality` |
| `/cross/cross_01/mortality` | 観測リンク（あれば） | `/observation/{id}` |

---

## 8. 投票・経済（20 / 22）

| ルート | 操作 | 期待 |
|--------|------|------|
| `/vote` | 投票ボタン | 投票済み表示 |
| `/economy/shop` | 購入（残高不足時 disabled） | PT 不足時ボタン無効 |

---

## 9. 設定・UI・機器・ビルダー（12 / 13 / 16 / 17 / 18 / 21）

| ルート | 操作 | 期待 |
|--------|------|------|
| `/settings` | 各サブリンク | 遷移可 |
| `/settings/ui-template` | テンプレ適用 | 選択状態更新 |
| `/env/shelf` | — | 機器一覧 or 空 |
| `/builder` | — | キャンバス・lint |
| `/component/photo-analysis` | タグ承認 | 承認メッセージ |
| `/admin/karma` | — | L4 サマリー（管理者） |

---

## 10. 回帰（全 path ナビ）

- [ ] ヘッダーから ホーム・観測・マーケット・掲示板・好み・プロフィール を Tab 巡回
- [ ] フッター「設定」「Streamlit」リンク
- [ ] 各画面で raw エラー文字列が露出しない
- [ ] ユーザー文言に「未実装」「WIP」が無い

---

## 11. 自動テスト（AI 完了分 · ユーザー実行可）

```powershell
cd 指示/it-hercules-laboratory
docker compose --profile test run --rm test
cd apps/web && npm install && npm test
```

---

*v1 · 2026-06-09 · HUMAN-IMPL-SIGNOFF 後のユーザー打鍵用*
