---
slice_id: 01-MICRO-error-002
type: error-code
http_status: 401
source_routes:
  - GET /api/v1/auth/session
  - POST /api/v1/auth/verify
catalog_ref: ../../エラーカタログ-v1.md#401-認証失敗
---

# 01-MICRO-error-002 — HTTP 401（認証失敗 · トークン/セッション）

- **owner**: auto
- **type**: error-code
- **inputs**: WorkOrder `01-MICRO-error-002` · `GET /session` · `POST /verify` · [`エラーカタログ-v1.md`](../../エラーカタログ-v1.md) §401
- **acceptance**: 401 の発生条件 · detail メッセージ · UI 導線 · エラーカタログ行 · impl 引用 · テスト参照

## 概要

#01 auth router の **401 Unauthorized** は (1) **verify 失敗**（トークン不正/期限切れ/使用済みの集約）と (2) **session 解決失敗**（opaque token 欠落/不明）の 2 系統。IHL は legacy の verify 400 分岐を **401 集約**（DET §7 P3）。

## 発生 route 一覧

| route | auth | 主な 401 トリガ | detail |
|-------|------|-----------------|--------|
| `POST /api/v1/auth/verify` | public | token 不正 · TTL 超過 · 使用済み · 空 | `INVALID_OR_EXPIRED_TOKEN` |
| `GET /api/v1/auth/session` | token header | `Authorization` / `X-IHL-Session` 欠落 · 不明 token | `AUTH_REQUIRED` |

### verify 詳細

| 条件 | detail | impl |
|------|--------|------|
| `verify_magic_link` が `None` | `INVALID_OR_EXPIRED_TOKEN` | `auth.py:83-85` |
| 同一 token 再 verify | 同上（`pop` 済） | `test_it_01_05_verify_token_reuse_401` |

### session 詳細

| 条件 | detail | impl |
|------|--------|------|
| token 抽出失敗 | `AUTH_REQUIRED` | `auth.py:102-105` |
| `resolve_actor_id` が `None` | `AUTH_REQUIRED` | `auth_session` + deps |

## detail 形式

```json
{ "detail": "INVALID_OR_EXPIRED_TOKEN" }
```

```json
{ "detail": "AUTH_REQUIRED" }
```

- **型**: 常に **string**
- **機械コード**: 英大文字スネーク（UI は humanize で 1 行日本語 · FR-LOGIN-10）

## ユーザー向け文言（Web）

| detail | 画面 | 表示方針 |
|--------|------|----------|
| `INVALID_OR_EXPIRED_TOKEN` | `/login?token=` 自動 verify 失敗 | 「リンクが無効または期限切れです。メールを再送信してください」（verify_error → idle · 遷移設計 §4.3） |
| `AUTH_REQUIRED` | 保護 API / session 確認 | 未ログイン → `/login` 誘導（FR-LOGIN-07 · FR-LOGIN-08） |
| dev ボタン失敗 | sent ステップ | バナー `role="alert"` · 再送信導線 |

## テスト参照

| test_case_id | ファイル | 検証内容 |
|--------------|----------|----------|
| IT-01-04 | `tests/unit/test_auth.py` | 無効 token verify → 401 |
| IT-01-05 | 同上 | token 再利用 → 401 |
| IT-01-10 | 同上 | session 無 token → 401 |
| FR-LOGIN-08 | UAT-01-06 | 401 導線 review |
| FR-LOGIN-04 | UT-01-04 | store 層 verify |

## impl 引用

```81:86:apps/api/routes/auth.py
@router.post("/verify")
def auth_verify(body: MagicLinkVerifyRequest) -> dict[str, Any]:
    result = get_auth_session_store().verify_magic_link(body.token.strip())
    if not result:
        raise HTTPException(status_code=401, detail="INVALID_OR_EXPIRED_TOKEN")
    return {"status": "authenticated", **result}
```

```96:106:apps/api/routes/auth.py
@router.get("/session")
def auth_session(
    authorization: str | None = Header(default=None),
    x_ihl_session: str | None = Header(default=None, alias="X-IHL-Session"),
) -> dict[str, Any]:
    token = extract_session_token(authorization, x_ihl_session)
    actor_id = resolve_actor_id(token)
    if not actor_id:
        raise HTTPException(status_code=401, detail="AUTH_REQUIRED")
    return {"status": "ok", "actor_id": actor_id}
```

## エラーカタログ整合

正本: [`エラーカタログ-v1.md`](../../エラーカタログ-v1.md) §「401 認証失敗」。契約オラクル: `node scripts/ihl-contract-oracle.mjs --feature 01 --check`（register 400 · verify/session 401 と一致）。

## 関連スライス

- API: [`post-api-v1-auth-verify.md`](../api/post-api-v1-auth-verify.md) · [`get-api-v1-auth-session.md`](../api/get-api-v1-auth-session.md)
- Schema: [`magiclinkverifyrequest.md`](../schema/magiclinkverifyrequest.md)
