# 04 ホーム — 詳細設計 v1（ダッシュボード API · 状態機械）

> **ステータス**: 草案 v1 · Batch 3 · 人間レビュー待ち
> **前提**: [`04-ホーム画面.md`](./04-ホーム画面.md) · ADR-H-14 文脈バー

---

## 1. ホーム状態機械

| 状態 | 条件 | 表示 |
|------|------|------|
| `loading` | dashboard 取得中 | スケルトン |
| `ready` | 200 OK | ヒーロー + CTA 2 + ナビ |
| `degraded` | 部分 API 失敗 | 近似値 + 再試行 |
| `onboarding_tour` | 初回保存直後 | driver.js オーバーレイ |
| `error` | 全 API 失敗 | エラー + 再読込 |

---

## 2. API 契約

| メソッド | ルート | 内容 |
|---------|--------|------|
| `GET` | `/api/home/dashboard` | handle · platinum · contribution_preview · unread · mini_map |

### 2.1 主 CTA（IHL mock 準拠）

| CTA | 遷移先 | FR |
|-----|--------|-----|
| 観測をはじめる | `/observation/input` | FR-HOME-01 |
| 検索 | `/observation/search` | FR-HOME-02 |

---

## 3. 文脈バー（ADR-H-14）

`observation_context` + `target_scope` をヘッダに表示 · 05 入力と同期。

---

## 4. 設計ゲート

要件 ☑ · **詳細 = 本 doc** · 遷移 ☑ · UI ☑（`ihl-01-nav-home.png`）

*Batch 3*
