# #04 ホーム — エラーカタログ v1

> **合図**: `IHL-DOC-REMED MAD` · **正本**: [`契約レジスタ-v1.yaml`](./契約レジスタ-v1.yaml) · 遷移: [`遷移辞書-v1.json`](./遷移辞書-v1.json)

---

## HTTP エラー（route 契約）

`GET /api/v1/home/summary` は **errors: []**（契約オラクル 1/1 PASS）。

| HTTP | 条件 | 備考 |
|------|------|------|
| — | route 定義上 4xx/5xx なし | 内部例外は catch → 近似値（NF-H-03） |

---

## フォールバック（200 · 非エラー応答）{#fallback}

| 条件 | 挙動 | detail | req |
|------|------|--------|-----|
| parquet 不在 | `obs_count = "—"` | raw エラー非表示 | NF-H-03 · H-032 |
| `count_captures` 例外 | `obs_count = "3"` | 近似 · Traceback 非表示 | NF-H-03 |
| schedule 構築例外 | `today_lines` 既定 3 行 | merge_today_lines | OBS-RX-UX-11 |

---

## ギャップ（設計のみ · 粉飭禁止）

| 項目 | req | 備考 |
|------|-----|------|
| 司法インボックス API エラー UI | H-040–H-044 | #11 境界 · deferred |
| リッチダッシュロード失敗 | H-033 | UI 層 · gap |
| legacy 5 カード不一致 | H-012 | IHL 4 カードを正 · gap |

---

## オラクル整合

`node scripts/ihl-contract-oracle.mjs --feature 04 --check` — **1/1 PASS**
