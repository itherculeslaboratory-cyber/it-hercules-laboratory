# 13 データ取得元 — 詳細設計 v1（collector · device registry）

> **ステータス**: 草案 v1 · Batch 5 · ADR-device-driver-registry

---

## 1. collector 契約

```yaml
data_source:
  id: uuid
  kind: manual | switchbot | placement | file_import
  driver_ref: string
  status: active | paused | error
  last_sync_at: ISO8601
```

---

## 2. API

| メソッド | ルート |
|---------|--------|
| `GET` | `/api/devices` |
| `POST` | `/api/devices` |
| `PATCH` | `/api/devices/:id` |
| `POST` | `/api/devices/:id/sync` |

---

## 3. 設計ゲート

要件 ☑ · **詳細 = 本 doc** · 遷移 ☑ · UI ☑（`ihl-13-device-registry.png`）

*Batch 5*
