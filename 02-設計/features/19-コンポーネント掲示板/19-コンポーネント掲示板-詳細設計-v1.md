# 19 コンポーネント掲示板 — 詳細設計 v1

> **ステータス**: **草案 v1 · 人間レビュー待ち**
> **作成日**: 2026-06-09
> **正本**: [`19-コンポーネント掲示板.md`](./19-コンポーネント掲示板.md) · [`05-GitHub運用-コンポーネント掲示板.md`](../../05-GitHub運用-コンポーネント掲示板.md)

---

## 1. IHL 正本（legacy file-board 非移植）

| 層 | 正本 | 用途 |
|----|------|------|
| 議論 | GitHub Discussions / PR | 意図・廃止・採用 |
| 履歴 | R2 component_event | INSERT ONLY |
| 索引 | `BOARD.md` + repo path | 人間・AI 参照 |

## 2. データ契約（草案）

```typescript
interface ComponentBbsAnchor {
  component_id: string; // C-USB UUID
  github_discussion_url: string;
  last_r2_event_id: string;
  canonical_md_path?: string; // 要件 md へのリンク
}
```

## 3. ユーザー向け UI（将来）

- **一般ユーザー**: `/improve/forks` 系譜閲覧のみ（現行）
- **開発者**: GitHub へディープリンク（アプリ内 WebView は v2）
- **ADR-H-14 文脈バー**: 「改善を提案」→ 掲示板 `07b` + `?context_route=`（コンポーネント単位 BBS ではなく画面文脈）

## 4. legacy REQ-018 との境界

| 項目 | civ-os legacy | IHL |
|------|---------------|-----|
| 1:1 MD↔スレ | `file_board_registry.csv` | **不採用** |
| RAG | `file_board` 種別 | GitHub + R2 索引 |

---

*草案 · 製品内コンポーネント BBS 一体化 UI はスコープ外（FR-19 既存）*
