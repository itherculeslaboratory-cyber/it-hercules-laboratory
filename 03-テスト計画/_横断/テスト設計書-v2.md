# テスト設計書 v2

> **ステータス**: **Batch 7 充填**（2026-06-09）  
> **正本 v1**: [`../../03-テスト計画/_横断/テスト設計書-v1-legacy.md`](../../03-テスト計画/_横断/テスト設計書-v1-legacy.md)

---

## 1. v2 追加 — 設計 contract テスト層

```text
                    ▲ 少
            ┌───────────────────┐
            │  E2E minimal      │  Implementation Sign-off 後
            ├───────────────────┤
            │  design contract  │  ← v2 新設（mock/遷移/4点）
            ├───────────────────┤
            │  pipeline smoke   │
            ├───────────────────┤
            │  contract (C-USB) │
            ├───────────────────┤
            │  unit             │
            └───────────────────┘
                    ▼ 多
```

| テスト | 実行 | 内容 |
|--------|------|------|
| `test_design_four_point` | `ihl-four-point-inventory.mjs` | 01〜20 の 4 点存在 |
| `test_design_mock_trace` | `ihl-mock-fr-trace.mjs` | mock↔トレース表 |
| `test_design_mock_coverage` | `ihl-mock-coverage.mjs` | 必須 PNG |
| `test_design_golden_pack` | 将来 pytest wrapper | 機能別設計パック JSON fixture |

---

## 2. 01〜20 golden チェックリスト

各機能: `req` + `detail` + `transition` + `ui` + 主 mock ≥1 = **設計パック完成**（委任 Go 前提）。

E2E（Playwright）は **HUMAN-IMPL-SIGNOFF 後**のみ追加（v1 方針維持）。

---

## 3. 21 翻訳横断

locale は 03 登録 + 12 設定。設計 contract は O2 mock 内包でカバー（専用 mock 不要）。

---

*Batch 7 · 2026-06-09*
