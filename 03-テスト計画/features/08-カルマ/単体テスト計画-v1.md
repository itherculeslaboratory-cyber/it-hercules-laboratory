# 08 カルマシステム — 単体テスト計画 v1（UT）

> **対象**: [`詳細設計-v2.md`](../../../02-設計/features/08-カルマ/詳細設計-v2.md) · 要件 [`08-カルマシステム.md`](../../../01-要件/08-カルマシステム.md)（`FR-KRM-*`）
> **層**: 単体（V-model 右腕最下層）· **retrofit**: 既存 `tests/unit/test_economy_logic.py` を主とし差分追加
> **方針**: Fib 決定性・二層 karma_event（INSERT ONLY）・snapshot 投影を純関数/ストア単位で検証

---

## 1. 対象ユニット

| ユニット | パス |
|----------|------|
| `fib_tier_delta` | `libs/ihl/economy/economy_logic.py` |
| `EconomyStore.record_fee_unpaid` / `karma_snapshot` | 同上 |
| `EconomyStore.purchase_shop_item`（免罪符 PT） | 同上 |

---

## 2. テストケース

| TC ID | 対象 FR | 内容 | 期待 | 状態 |
|-------|---------|------|------|------|
| **UT-08-01** | FR-KRM-03 | `fib_tier_delta(1/5/10)` | 1 / 5 / 55（決定的） | 既存 `test_fib_sequence` |
| **UT-08-02** | FR-KRM-03 | `fib_tier_delta(0)` | 0（境界 · 増加なし） | 追加 |
| **UT-08-03** | FR-KRM-08/09 | `record_fee_unpaid(months=4)` | `fib_delta==3` · karma_event layer=count | 既存 `test_fee_unpaid_records_fib` |
| **UT-08-04** | FR-KRM-09 | karma_event は append（INSERT ONLY） | event id 払い出し · UPDATE/DELETE なし | 追加 |
| **UT-08-05** | FR-KRM-10 | `karma_snapshot` after fee_unpaid(2) | count==fib(2)==1 · value≥128 · trend | 既存 `test_karma_snapshot_after_fee_unpaid` |
| **UT-08-06** | FR-KRM-03 | snapshot で value 層と count 層が分離集計 | value/count 別々に Σ | 追加 |
| **UT-08-07** | FR-KRM-07 | `purchase_shop_item(indulgence_7d)` | status=purchased · pt_event 追記 | 既存 `test_shop_purchase_writes_pt_event` |
| **UT-08-08** | FR-KRM-03 | Fib 上限超（months>10）クリップ | FIB_SEQUENCE[9]==55 で飽和 | 追加 |

---

## 3. 合否基準

- 全 UT green。Fib は決定的（同入力→同出力）。karma_event は二層分離で append のみ。
- gap（25 日バッチ FR-KRM-04/05・BAN FR-KRM-06・値クリップ FR-KRM-02）は本層では検証せず **RTM `status=gap`**。

*v1 · 2026-06-10 · V-WAVE-09-08-TD-UT*
