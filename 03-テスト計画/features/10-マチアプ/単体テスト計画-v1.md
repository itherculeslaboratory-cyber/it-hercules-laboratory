# 10 マチアプ — 単体テスト計画 v1（UT）

> **対象**: [`詳細設計-v2.md`](../../../02-設計/features/10-マチアプ/詳細設計-v2.md) · 要件 `FR-MCH-*`
> **層**: 単体 / route ハンドラ（TestClient 経由）· **retrofit**: 既存 `tests/unit/test_match.py`
> **方針**: pairwise pair/vote · preference_event INSERT · プライバシー（hash · 非永続）を検証

---

## 1. 対象ユニット

| ユニット | パス |
|----------|------|
| `match_pair` / `match_vote` | `apps/api/main.py` |
| `write_preference_event` | `libs/ihl/core/event_store.py` |

---

## 2. テストケース

| TC ID | 対象 FR | 内容 | 期待 | 状態 |
|-------|---------|------|------|------|
| **UT-10-01** | FR-MCH（pair） | `GET /match/pair` | status ok · pair に left/right | 既存 `test_match_pair_and_vote` |
| **UT-10-02** | FR-MCH-05 | `POST /match/vote` | preference_event_id 払い出し | 既存 同上 |
| **UT-10-03** | NFR-MCH-02 | persisted event に dimension_matrix 無 | 非永続（プライバシー） | 追加 |
| **UT-10-04** | NFR-MCH-02 | voter_handle は user_id_hash 化 | 生 handle 非保存 | 追加 |
| **UT-10-05** | FR-MCH-02 | choice 不正値は left に正規化 | 落とさず recorded | 追加 |
| **UT-10-06** | FR-MCH（404） | 不在 pair_id の vote | 404 | 追加 |

---

## 3. 合否基準

- 全 UT green。preference_event INSERT・プライバシー（hash + 非永続）・choice 正規化を担保。
- ValueCheck/MatchApp（gap）は本層では検証せず RTM 追跡。

*v1 · 2026-06-10 · V-WAVE-11-10-TD-UT*
