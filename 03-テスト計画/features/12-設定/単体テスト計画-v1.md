# 12 設定 — 単体テスト計画 v1（UT）

> **対象**: [`詳細設計-v2.md`](../../../02-設計/features/12-設定/詳細設計-v2.md) · 要件 `FR-SET-*` · `NFR-SET-*`
> **層**: 単体 / route ハンドラ（TestClient）· store 層 · **retrofit**: 既存 `tests/unit/test_preferences_api.py` · `tests/unit/test_api.py`
> **方針**: preferences PATCH · locale 統合 · Truth PII 方針 · 許可フィールドのみ更新を検証

---

## 1. 対象ユニット

| ユニット | パス |
|----------|------|
| `PreferencesStore` | `libs/ihl/identity/preferences_store.py` |
| me routes | `apps/api/routes/me.py` |
| settings alias | `apps/api/main.py`（`GET /api/v1/settings`） |
| `TradePiiSession` | `libs/ihl/governance/pii.py` |

---

## 2. テストケース

| TC ID | 対象 FR/NFR | 内容 | 期待 | 状態 |
|-------|-------------|------|------|------|
| **UT-12-01** | FR-SET-11/15 | `GET /me/preferences` 既定 | `language==ja` | 既存 `test_preferences_get_defaults` |
| **UT-12-02** | FR-SET-15 | `PATCH language` → settings 反映 | `GET /settings` 同期 | 既存 `test_preferences_patch` |
| **UT-12-03** | FR-SET-09 | PATCH 許可フィールドのみ | `default_device_id` 保存 | 既存 `test_preferences_patch` |
| **UT-12-04** | FR-SET-09/NFR-SET-01 | 既定 `truth_pii_policy` | `no_plaintext` | 追加 |
| **UT-12-05** | FR-SET-17/18 | `POST /settings/pii-session` | `mode: session_only` | 追加 |
| **UT-12-06** | NFR-SET-01 | settings に API key 無 | キー不在 | 追加 |
| **UT-12-07** | FR-SET-02/03 | 論文/固体 LLM PATCH | 404/未登録 | gap |
| **UT-12-08** | FR-SET-04/05 | dev/admin トグル API | 未登録 | gap |

---

## 3. 合否基準

- 全 UT green（既存 + retrofit 追加分）。preferences 投影 · locale PATCH · PII 方針を担保。
- LLM · dev/admin · trade_pii CRUD（gap）は本層では検証せず RTM 追跡。

*v1 · 2026-06-10 · V-WAVE-13-12-TD-UT*
