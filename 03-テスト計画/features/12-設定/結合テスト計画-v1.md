# 12 設定 — 結合テスト計画 v1（IT）

> **対象**: [`詳細設計-v2.md`](../../../02-設計/features/12-設定/詳細設計-v2.md) · 要件 `FR-SET-*`
> **層**: 結合（route × PreferencesStore × event root · PII session × event store）
> **方針**: PATCH → ファイル投影 → GET 読戻し · pii-session → pii_access_event 連鎖

---

## 1. 結合範囲

| 結合 | 構成 |
|------|------|
| route × store | `PATCH /me/preferences` × `PreferencesStore.patch` × `settings.json` |
| settings エイリアス | `GET /settings` × 同一 store |
| PII session × audit | `POST /settings/pii-session` × `write_pii_access_event` |

---

## 2. テストケース

| TC ID | 対象 FR | シナリオ | 期待 | 状態 |
|-------|---------|----------|------|------|
| **IT-12-01** | FR-SET-15 | PATCH language → GET settings | 同一 actor で一致 | 既存連鎖 |
| **IT-12-02** | FR-SET-09 | 複数 PATCH で updated_at 更新 | 永続化 · マージ | 追加 |
| **IT-12-03** | FR-SET-17 | pii-session → get_masked | session_active | 追加 |
| **IT-12-04** | FR-SET-16〜18 | trade_pii PATCH → counterparty GET | 設計契約 | gap |
| **IT-12-05** | FR-SET-19 | #06 申込 → completeness 誘導 | 設定へ 1 クリック | xref #06 |

---

## 3. 合否基準

- IT green。preferences 永続化 · settings エイリアス · PII セッション監査が安定。
- trade_pii CRUD · マーケット誘導無しは gap/xref で RTM 追跡。

*v1 · 2026-06-10 · V-WAVE-13-12-TD-IT*
