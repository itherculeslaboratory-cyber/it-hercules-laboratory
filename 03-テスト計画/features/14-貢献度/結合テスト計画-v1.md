# 14 貢献度 — 結合テスト計画 v1（IT）

> **対象**: [`詳細設計-v2.md`](../../../02-設計/features/14-貢献度/詳細設計-v2.md) · 要件 `FR-CONTRIB-*`
> **層**: 結合（event store × economy store × API route）
> **方針**: contribution_event → contribution_total → `/api/v1/contribution` · `profile/metrics` の連鎖を検証

---

## 1. 結合範囲

| 結合 | 構成 |
|------|------|
| ストア × event store | `write_contribution_event` → `economy/contribution_event` 永続 |
| route × ストア | `GET /api/v1/contribution` → `contribution_total` |
| profile × ストア | `GET /api/v1/profile/metrics` → contribution.value + breakdown |

---

## 2. テストケース

| TC ID | 対象 FR | シナリオ | 期待 | 状態 |
|-------|---------|----------|------|------|
| **IT-14-01** | FR-CONTRIB-10 | `GET /api/v1/contribution` 初期 | 200 · total · badges キー存在 | 既存 `test_contribution_summary` |
| **IT-14-02** | FR-CONTRIB-09/ADR-H-08 | `GET /profile/metrics` contribution 部分 | value · breakdown キー存在 · karma と独立 | 追加 |
| **IT-14-03** | FR-CONTRIB-01/02 | event 追記後に contribution API | total が delta 分増加 | 追加 |
| **IT-14-04** | FR-CONTRIB-10 | contribution 応答に nodeId 不要（ユーザー要約） | actor_id クエリで集計 | 追加 |
| **IT-14-05** | FR-CONTRIB-03 | 上流 10% 分配後の祖先 score（設計） | 祖先加算（gap · legacy 未移植） | gap |

---

## 3. 合否基準

- IT green。API 契約が安定。event 追記が total に反映。
- 上流分配・breakdown 実集計（P1/P2）は gap として **RTM `status=gap`**。

*v1 · 2026-06-10 · V-WAVE-15-14-TD-IT*
