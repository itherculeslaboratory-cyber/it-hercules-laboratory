# 15 データ設計 — 単体テスト計画 v1（UT）

> **対象**: [`詳細設計-v2.md`](../../../02-設計/features/15-データ設計/詳細設計-v2.md) · 要件 [`15-データ設計.md`](../../../01-要件/15-データ設計.md)（`FR-DATA-*`）
> **層**: 単体（schema 検証 · registry ロジック）
> **retrofit**: 既存 `tests/contract/test_schema_validator.py` · `tests/unit/test_schema_registry.py` を正とし差分のみ追加

---

## 1. 対象ユニット

| ユニット | パス |
|----------|------|
| `SchemaRegistry` | `libs/ihl/core/schema_validator.py` |
| `validate_instance` | 同上（便利関数） |
| schema 存在確認 | `schema_exists` · `_normalize_ref` |

---

## 2. テストケース

| TC ID | 対象 FR | 内容 | 期待 | 状態 |
|-------|---------|------|------|------|
| **UT-15-01** | FR-DATA-01 | `core_subset` 最小 instance 検証 | `ok=True` | 既存 `test_core_subset_valid_minimal` |
| **UT-15-02** | FR-DATA-01 | 余剰フィールド拒否 | `ok=False` | 既存 `test_core_subset_rejects_extra_fields` |
| **UT-15-03** | FR-DATA-10/11 | `run_info` fixture 検証 | manifest 必須列充足 | 既存 `test_run_info_valid_fixture` |
| **UT-15-04** | FR-DATA-05 | `enum_ref` → sex dictionary 解決 | enum に male/female | 既存 `test_enum_ref_resolves_sex` |
| **UT-15-05** | NFR-DATA-02 | schema ref 正規化 | `.schema.yaml` 付与 | 既存 `test_normalize_ref_adds_suffix` |
| **UT-15-06** | NFR-DATA-03 | 不在 schema ref | `schema_exists` false | 既存 `test_schema_exists_helper` |

---

## 3. 合否基準

- 全 UT green。schema 検証が core_subset · run_info · enum_ref を assert。
- pipeline exists-write は component 単体テスト（`test_r2_io` 等）へ委譲 · RTM `xref`。

*v1 · 2026-06-10 · V-WAVE-16-15-TD-UT*
