# IHL Tier A 突き合わせ監査 — #06 / #07 / #16 / #17 / #23（読み物品質）

> **種別**: Tier A read-depth 監査（要件→設計→テスト→コードの整合を**読んで**突き合わせる）
> **対象**: #06 マーケット · #07 掲示板 · #16 UIbuilder · #17 UI選択画面改善 · #23 GMO 銀行振込判定
> **日付**: 2026-06-10 · **監査ID**: 20260610-ihl-tiera-cross-audit
> **方針**: 正直（cheerlead しない）。parity script の PASS だけでは合格としない。コード本文・テスト assert・RTM を実読して矛盾を拾う。
> **前提**: 5 機能とも `ihl-design-impl-parity-check.mjs` は **PASS（0 FAIL）**。salvage-adapt 4 機能（#06/#07/#16/#17）は stays-only テスト撤去 + `test_must_assert` 追加済み（post-remediation）。本監査はその **質** を検証した。
> **注記**: 本環境に Python インタプリタが PATH 上に無いため、pytest の再実行はできず、**parity checker の PASS + テストファイル静的精読**で緑性を確認した（テスト assert は全機能で実体ある主張）。

---

## エグゼクティブサマリ（総合判定）

**総合判定: CONDITIONAL PASS（条件付き合格）**

- **突き合わせの骨格は健全**: 5 機能すべてで 要件 FR → 詳細設計 v2 → RTM → 実装パス → テストの鎖が**実在**し、parity も PASS。salvage-adapt の `test_must_assert`（listing/trade・thread/board・theme_pack/builder・language/settings・transfer_code/matched_expected_ids/tier）は **形だけでなく本物の assert** に裏打ちされている（stays-only 文字列チェックの残骸は無し）。
- **ただし「PASS = 出荷品質」ではない**。読み込むと以下の実害級・品質級の弱点が出る:
  - **#23**: 複数 pending の照合が **全件 matched**（`match_pending_expected_from_va_transaction`）で、要件 FR-GMO-02 / NFR-GMO-02「**1 件に決定・二重 matched 防止**」と**現状で矛盾**（将来 gap ではなく現装の不整合）。
  - **#07**: `list_threads` のカテゴリ分離が `general` と `component`（両方 `board_kind=other` に正規化）で**相互リーク**。テストは paper/rant のみで穴を踏んでいない。
  - **#16**: NFR-16-01「R2 INSERT ONLY」を assert するテストが無く、`save_pack(overwrite=True)` が R2 backend を**強制上書き**する逃げ道を持つ。
  - **#17**: IHL 側に **#17 固有実装が無い**（#12 settings/preferences の流用のみ）。テストは #12 のエンドポイントを叩いているだけで #17 ロジックは検証していない（設計上は gap 透明化済み）。
  - **#23**: 本番ブロッカーが複数（live・本番署名・公開URL・部分/過入金・issueCoin・日時FIFO）。stub/stg は緑だが本番化は遠い。
- **判定内訳**: #06 = **PASS** / #07 = **CONDITIONAL** / #16 = **CONDITIONAL** / #17 = **CONDITIONAL** / #23 = **CONDITIONAL**。

**Top 5 是正（優先順）**

| # | 優先 | 機能 | 内容 |
|---|------|------|------|
| 1 | P1 | #23 | `match_pending_expected_from_va_transaction`（`gmo_reconciliation_store.py` L166-180）が同コード+同額の pending を**全件** matched。NFR-GMO-02 違反。最低でも単一マッチ化 or idempotency ガード、理想は §2.5.3 日時 FIFO 実装。 |
| 2 | P1 | #07 | `board_store.list_threads`（L94-116）の `general`↔`component` 相互リーク（共に `other` 正規化）。フィルタを category 主にするか、isolation テストを general/component で追加。 |
| 3 | P2 | #16 | NFR-16-01 INSERT ONLY を assert するテスト（同一 `theme_pack_id` 再保存で `R2NoOverwriteError`）を追加し、`save_pack(overwrite=True)` の R2 強制上書き経路を封じる/ガードする。 |
| 4 | P2 | #23 | 詳細設計-v2.md の体裁不良（`## 5` 見出し重複 / 冒頭「v2 確定ドラフト」末尾「v2.1」の版不一致）と、§2.2 登録時衝突 alternate-slice が `derive_transfer_code` に**未実装**である旨を設計に明記。 |
| 5 | P3 | #17 | RTM/設計に「IHL 側 #17 固有実装は無し（テストは #12 settings を流用）」を明示し、カバレッジを過大に見せない注記を入れる。 |

> 本監査では**コードは修正していない**（HUMAN-IMPL-SIGNOFF 済み retrofit 領域 + 設計ゲートのため、報告 + 任意修正リストに留める）。wave queue の `[x]` 化・人間ゲートの偽装は**していない**。

---

## #06 マーケット — 判定: **PASS**

### 整合表（主要 FR）

| req_id | 要件要旨 | 設計 v2 | テスト | コード | 整合 |
|--------|----------|---------|--------|--------|------|
| FR-MKT-02 | 7状態・許可辺のみ・不正409 | §4.1 | `test_market_state::test_listing_allowed/forbidden`・`test_market_routes::it_06_05/06` | `market_state.transition_listing` | ✅ |
| FR-MKT-03 | 現在状態=イベント末尾、無→unlisted | §2.1 | `test_ut_06_05_no_event_is_unlisted` | `current_listing_state` | ✅ |
| FR-MKT-13 | 取引成立=配送+評価、stage後退不可 | §5 | `test_ut_06_06_trade_stage_cannot_regress`・`it_06_07` | `advance_trade_stage`（`to_stage<current`→409） | ✅ |
| FR-MKT-07 | 成立後8%貢献費 | §2.3 §6 | `it_06_08_gmo_transfer_8pct`（amount=price×8%） | `gmo_transfer` | ✅ |
| FR-MKT-04 | オークション自動決着 | §7 P3 | UAT review | 未配線 | ⚠ gap（明示） |
| FR-MKT-14/15 | 抽選・プラチナ順 | §7 P1/P2 | UAT review | 未配線（草案） | ⚠ gap（明示） |
| NFR-MKT-03 | 出品状態書込の認可 | §6 §7 P7 | IT review | dev は actor_id 明示のみ | ⚠ gap（明示） |

### 強み

- 設計 §4.1 の `LISTING_TRANSITIONS` がコード `market_state.py` の辞書と**逐語一致**。読み物として設計→実装の追跡が容易。
- 取引成立 ≠ マッチング（Stage 3）の概念が要件§11.0.1 → 設計§5 → `advance_trade_stage` 後退禁止テストまで一貫。
- `test_must_assert: listing/trade` は実 assert（`to_state=="delisted"`・`board["step"]=="private_board"`）に裏打ち。stays-only 残骸なし。

### 弱点（具体）

- **gap が広い**（FR-MKT-04/14/15 · NFR-MKT-03/06）。ただし RTM・詳細設計§7.2（P1〜P7）に `status=gap/草案/human` で**正直に**列挙されており、読み物の透明性は保たれている。
- `apps/api/routes/market.py` の `transition`・`match` ルートに**認可が無い**（`actor_id` を body で受けるだけ）。NFR-MKT-03 gap と整合するが、本番では未認可書込の穴。
- `gmo_transfer` の `obligor_user_id` 既定値 `"user-demo"`（route 既定）— stub なので実害なしだが、任意 listing の transfer code を誰でも取得可能。

### salvage-adapt 評価

- **本物**。テストは状態機械・取引ステージ・8% 額を実際に動かして検証。`forbid_stays_only=true` の意図どおり、stays-only テストは存在しない。

---

## #07 掲示板 — 判定: **CONDITIONAL**

### 整合表（主要 FR）

| req_id | 要件要旨 | 設計 v2 | テスト | コード | 整合 |
|--------|----------|---------|--------|--------|------|
| FR-BBS-05 | スレ作成・投稿追記 | §3.2-3.4 | `test_thread_create_and_post`・`ut_07_04` | `create_thread`/`append_post` | ✅ |
| FR-BBS-14 | 入口4つ・正規化 | §3.1 §2.2 | `ut_07_02_normalize_board_kind` | `normalize_board_kind` | ✅ |
| FR-BBS-15 | 論文 case_chip | §2.1 | `ut_07_03_case_chip_preserved` | `create_thread(case_chip)` | ✅ |
| FR-BBS-06 | カテゴリ分離 | §3.1 | `ut_07_06_category_isolation`（**paper/rant のみ**） | `list_threads` | ⚠ **穴あり** |
| NFR-BBS-01 | INSERT ONLY 追記 | §4 §6 | `ut_07_04`・`ut_07_05` | append + projection | ✅ |
| FR-BBS-01/02/04/08/09 | file-board linkage（REQ-018） | §7 P1 | UAT review | IHL 不採用 | ⚠ gap（明示） |

### 強み

- 設計 §2.2 の `_CATEGORY_MAP` がコードと一致。`validate_board_event` の未知 kind 弾きがテスト済み。
- `test_must_assert: thread/board` は実 assert（`thread_id` 一致・`post_count==2`）。本物。

### 弱点（具体）

- **【実害級バグ】`libs/ihl/governance/board_store.py` L94-116 `list_threads`**: フィルタが
  ```
  if row.board_kind != board_kind and row.category != category: continue
  ```
  の **OR 包含**。`general`→`other`、`component`→`other` と**同一 board_kind に正規化**されるため、`list_threads("general")` に **component スレが混入**（`board_kind=="other"` が一致して残る）。逆も同様。FR-BBS-06「別カテゴリは混ざらない」を `general`/`component` で破る。
- **テストが穴を踏んでいない**: `ut_07_06_category_isolation` は board_kind が別物の **paper vs rant** だけを検証。general↔component の組合せが未テストのためバグが緑のまま通過。
- file-board linkage（REQ-018 中核 FR 群）が IHL で**全て gap/不採用**。設計上の決定として正直だが、要件 16 件中 9 件が gap/xref/deferred で、#07 の「実装率」は見かけより低い。

### salvage-adapt 評価

- テスト自体は本物（stays-only なし）。ただし**カバレッジに分離バグの死角**がある点が CONDITIONAL の理由。

---

## #16 UIbuilder — 判定: **CONDITIONAL**

### 整合表（主要 FR）

| req_id | 要件要旨 | 設計 v2 | テスト | コード | 整合 |
|--------|----------|---------|--------|--------|------|
| FR-DTH-01 | ThemePack エンティティ | §2.2 | `test_save_and_load_pack`・`it_16_01/02` | `ThemePackStore.save/load_pack` | ✅ |
| FR-16-REFRAME-01 | 配置/デザインのみ第一級 | §1.1 §3.4 | `it_16_03_builder_canvas_save` | `save_canvas_manifest` | ✅ |
| FR-16-REFRAME-02 | catalog選択紐づけのみ | §2.1 | UT/UAT | `catalog/components.yaml`（lint未配線） | ⚠ partial |
| NFR-16-01 | R2 INSERT ONLY | §5 §6 | **無し** | `save_pack`（overwrite経路あり） | ⚠ **未検証** |
| FR-DTH-04 | world_default runtime適用 | §2.2 | IT | runtime CSS 未配線 | ⚠ gap |
| FR-16-REFRAME-05/06/07/08 | preview/lint/domain/Phase8分離 | §1.2 §5 | 各 | legacy salvage / platform | ⚠ gap 多数 |

### 強み

- ADR-H-01（UI-only 窄化）の意図に沿って **ThemePack + canvas のみ**を実装スコープに切り出し、残りは §7 parity 表（P1〜P8）で `existing/partial/gap` を明示。
- `test_must_assert: theme_pack/builder` は実 assert（round-trip・`manifest.nodes==nodes`）。本物。

### 弱点（具体）

- **NFR-16-01（INSERT ONLY）が無検証**: 設計の最重要 NFR にもかかわらず、同一 `theme_pack_id` 二重保存で `R2NoOverwriteError` になることを確認するテストが無い。
- **逃げ道の存在**: `theme_pack.py` L31-49 `save_pack(overwrite=True)` は、`R2NoOverwriteError` を握りつぶして `self.r2._backend.write_bytes(..., overwrite=True)` で**R2 を強制上書き**する。INSERT ONLY 原則（ProjectRules / NFR-16-01）に対する明確な穴。RTM は NFR-16-01 を `partial 版管理` とするが、上書き経路の存在自体が要注意。
- **実装が薄い**: 8 個の P 項目中 5 個が gap（ScreenDef IDE・fork v2・route-matrix・capability help・lint）。UIbuilder の体感機能の大半は IHL 未着。設計は正直だが「機能が動く」状態には程遠い。

### salvage-adapt 評価

- テストは本物。ただし NFR の柱（INSERT ONLY）が未検証で、overwrite 経路が残る点が CONDITIONAL の理由。

---

## #17 UI選択画面改善 — 判定: **CONDITIONAL**（最も薄い）

### 整合表（主要 FR）

| req_id | 要件要旨 | 設計 v2 | テスト | コード | 整合 |
|--------|----------|---------|--------|--------|------|
| FR-17-06 | 一般3クリック入口 | §7 | `test_preferences_get/patch`（**#12 流用**） | `settings_get`（#12 共有） | ⚠ partial |
| FR-17-01 | Worldテンプレ選択 | §2.1 | gap | world-templates API 無し | ⚠ gap |
| FR-17-02 | 必須機能チェック | §4 | gap | legacy WorldRoutingManager.tsx | ⚠ gap |
| FR-17-03 | template level 3種 | §2.1 | gap | 設計のみ | ⚠ gap |
| FR-17-04 | LayerSelector L0-L3 | §2.2 | gap | civ-os session のみ | ⚠ gap |
| FR-17-05 | 投票自然淘汰接続 | §1.2 | gap | #20 未配線 | ⚠ gap |

### 強み

- 設計 v2・RTM ともに「IHL は単一 Streamlit · World 文化は civ-os 固有」「現行は settings/preferences の**暫定入口**のみ」と **gap を全面透明化**。誇張なし。
- `test_must_assert: language/settings` は `test_preferences_api.py` の実 assert（`language=="ja"`・PATCH 反映）で満たされている。

### 弱点（具体）

- **#17 固有の実装が存在しない**。IHL 側の「実装」は #12 設定（`/api/v1/settings`・`/api/v1/me/preferences`）の**流用**であり、`test_preferences_api.py` の UT-17-01/02 は docstring が #17 を名乗るが、検証しているのは #12 のエンドポイント。#17 ロジック（World テンプレ選択・必須機能ゲート・Layer 永続化）は**一行も**テストされていない（存在しないため）。
- RTM の FR-17-06 `partial` は「settings 暫定のみ」を意味するが、読み手が「#17 が部分実装」と誤読しやすい。**「#17 固有実装なし」**を明記すべき。

### salvage-adapt 評価

- stays-only テストは無い（良い）が、**そもそも #17 を検証するテストが無い**。cross-ref の整合性は取れているが、読み物としての実装深度はほぼゼロ。設計を「gap 透明化された設計成果物」として読む限りは矛盾なし。

---

## #23 GMO 銀行振込判定 — 判定: **CONDITIONAL**（薄い設計 + 本番ギャップ大）

### 整合表（主要 FR）

| req_id | 要件要旨 | 設計 v2 | テスト | コード | 整合 |
|--------|----------|---------|--------|--------|------|
| FR-GMO-05 | userId決定的transfer_code | §2 | `test_derive_transfer_code_deterministic`・`transfer_code_route` | `derive_transfer_code`（§2.2 と逐語一致） | ✅ |
| FR-GMO-01 | Webhook署名検証 | §3 | `webhook_signature_rejects/accepts`（401/202） | `gmo_webhook_security` | ✅ |
| FR-GMO-04 | R2 INSERT ONLY payment | §4 | `st_23_01_webhook_appends_store` | json store append | ✅ |
| FR-GMO-02 | コード+金額照合・複数は**1件に決定** | §4.2 §2.5.3 | `expected_payment_and_webhook_match`（**単一 pending のみ**） | `match_pending_expected_from_va_transaction`（**全件 matched**） | ❌ **矛盾** |
| NFR-GMO-02 | 決定的・二重matched防止 | §6 | 無し | idempotency TBD・複数 matched 可 | ❌ **矛盾** |
| FR-GMO-08/09 | 部分入金/過入金 | §4.2 | gap | 未実装 | ⚠ gap |
| FR-GMO-07 | 本番鍵・live証跡 | §1 | `live_tier_rejected` | `assert_stub_tier`（live→RuntimeError） | ✅ human gate |

### 強み

- `derive_transfer_code`（`gmo_transfer_code.py`）は要件 §2.2 S1〜S5（SHA-256 → uint24 BE → Base36 大文字 → 4pad/6trunc → `U-`）と**完全一致**。決定性テストあり。
- HMAC 署名の正否で 401/202 を分けるテストが実在。`assert_stub_tier`/`assert_bank_api_allowed` で live と stub の銀行 API 到達を二段ガード。`test_must_assert: transfer_code/matched_expected_ids/tier` は本物。
- 設計 v2 §4.1/§4.2 が「現行は複数可・日時未使用 / 要件 v1.5 の FIFO は gap」と**差分を明示**。

### 弱点（具体）

- **【実害級】複数 matched**: `gmo_reconciliation_store.py` L166-180 は条件一致の pending を**ループで全件 matched** にする。同一 `transfer_code` + 同額の pending が複数あると**全部消し込む**。要件 FR-GMO-02「§2.5.3 により**1 件**に決定」・NFR-GMO-02「二重 matched 防止」と**現状で矛盾**。これは「将来 gap」ではなく現装の不整合（設計 §4.1 は正直に「複数可」と認めているが、要件との乖離が残る）。
- **idempotency 未実装**: 同一 Webhook 再送に対する重複 matched ガードが無い（NFR-GMO-02 TBD）。
- **§2.2 登録時衝突の alternate-slice 未実装**: `derive_transfer_code` は衝突回避（+3 バイトオフセット）を**一切持たない**。要件は TBD としているが、実装には衝突検知の入口すら無い。
- **設計 doc 体裁不良**: `詳細設計-v2.md` に **`## 5` 見出しが 2 回**（「8% 貢献費フロー」と「component ITO」）。冒頭「v2 確定ドラフト」・末尾「v2.1」の**版表記不一致**。

### #23 本番前ブロッカー一覧

| # | ブロッカー | 種別 | 正本 |
|---|------------|------|------|
| B1 | live 実入金照合証跡 | **人間ゲート** | `P0-NEXT-GMO-LIVE-EXEC` / `POST-B8-GMO-05` |
| B2 | 本番 API 鍵投入 · ポータル登録 | **人間ゲート** | `POST-B8-GMO-04` |
| B3 | 本番署名形式の契約合わせ | **人間ゲート** | U-GMO-04 |
| B4 | Webhook 公開 URL · インフラ | 未着手 | U-GMO-05 |
| B5 | 複数 pending **日時 FIFO**（§2.5.3） | **実装 gap（要件矛盾）** | FR-GMO-02 / NFR-GMO-02 |
| B6 | 部分入金 残債減算（FR-GMO-08） | 実装 gap | §3.1 |
| B7 | 過入金 貢献費クレジット（FR-GMO-09） | 実装 gap | §06 §11.7.5 |
| B8 | issueCoin 要否（U-GMO-03） | 未確定（会計） | §22 |
| B9 | idempotency（二重 matched 防止） | 実装 gap | NFR-GMO-02 |
| B10 | 合算振込の複数 trade_ref 按分 | TBD | §2.5.5 |

### salvage-adapt 評価

- テストは本物（HMAC・決定性・matched_expected_ids を実検証）。ただし**現装の複数 matched が要件と矛盾**しており、stub であっても NFR-GMO-02 を満たさない点が CONDITIONAL の核。

---

## 監査メタ

| 項目 | 値 |
|------|-----|
| parity script | #06/#07/#16/#17/#23 すべて **PASS（0 FAIL）** |
| pytest 再実行 | 不可（本環境に Python が PATH 上に無い）→ parity + テスト静的精読で代替 |
| コード修正 | **無し**（retrofit + 設計ゲートのため報告に留める） |
| wave queue `[x]` 化 | **していない** |
| 人間ゲート偽装 | **していない**（B1〜B3 は人間ゲートとして温存） |

*Tier A 突き合わせ監査 v1 · 2026-06-10 · 20260610-ihl-tiera-cross-audit · 正直監査（cheerlead なし）*

---

## §P1 remediated（v1.1 · 2026-06-14）

| P1 | 機能 | 是正内容 |
|----|------|----------|
| 1 | #23 | `match_pending_expected_from_va_transaction` — 同一コード+同額の pending は **created_at 最古 1 件のみ** matched（FR-GMO-02 / NFR-GMO-02）。`test_match_pending_single_oldest_only` 追加。 |
| 2 | #07 | `board_store.list_threads` — フィルタを **category 完全一致**に変更（general↔component の board_kind=other 相互リーク解消）。`test_ut_07_06_general_component_isolation` 追加。 |

> idempotency（同一 Webhook 再送）・日時 FIFO の完全実装（§2.5.3）は P2/P3 として温存。live 人間ゲートは偽装していない。

## §P2/P3 remediated（v1.2 · 2026-06-14）

| P | 機能 | 是正内容 |
|---|------|----------|
| P2 | #16 | `save_pack(overwrite=True)` R2 強制上書き経路を削除 · NFR-16-01 `test_nfr_16_01_insert_only_rejects_duplicate_pack` 追加 |
| P2 | #23 | 詳細設計-v2 体裁修正（`## 5` 重複解消 · v2.1 版統一）· alternate-slice 未実装明記 · idempotency 要件明文化 |
| P3 | #17 | RTM `META-17-IHL` + 詳細設計 §0 に「IHL 固有実装なし（#12 settings 流用）」追記 |
| P2 | #23 code | `receive_webhook_and_match` — `raw_body_digest` 重複時再 match 禁止 · `test_webhook_idempotency_same_body_no_double_match` |

> 日時 FIFO（§2.5.3）· 部分/過入金（FR-GMO-08/09）· alternate-slice 実装 · live 人間ゲートは **未了**（偽装なし）。
